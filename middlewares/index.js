const validateBody = require('./validateBody');
const isValidId = require('./isValidID');
const autheticate = require('./autheticate');
const relocateAvatar = require('./relocateAvatar');
const upload = require('./upload');
module.exports = {
  validateBody,
  isValidId,
  autheticate,
  relocateAvatar,
  upload,
};
